NODE_ENV=development
HOST=127.0.0.1
PORT=3000
# Comma-separated browser origins. Empty means no cross-origin browser access.
CORS_ORIGINS=

# Used only by Prisma CLI validation; no database connection is made.
DATABASE_URL=postgresql://localhost:5432/akgebeya
